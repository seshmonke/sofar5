# 🎉 Swagger Setup - Интеграция Swagger в AssortiShop API

Этот документ описывает, как была настроена Swagger документация для API бэкенда.

## ✅ Что было сделано

### 1. Установлены зависимости
```bash
npm install swagger-ui-express swagger-jsdoc
npm install --save-dev @types/swagger-ui-express @types/swagger-jsdoc
```

**Установленные пакеты:**
- `swagger-ui-express` - для отображения интерактивного Swagger UI
- `swagger-jsdoc` - для генерации OpenAPI спецификации из JSDoc комментариев
- `@types/swagger-ui-express` - TypeScript типы
- `@types/swagger-jsdoc` - TypeScript типы

### 2. Создана конфигурация Swagger
**Файл:** `src/config/swagger.ts`

Содержит:
- Определение OpenAPI 3.0.0 спецификации
- Информацию о API (название, версия, описание)
- Определение серверов (development и production)
- Определение всех схем данных (Product, Category, PaginatedProducts и т.д.)

### 3. Интегрирована Swagger в приложение
**Файл:** `src/app.ts`

Добавлены:
- Импорт `swagger-ui-express` и конфигурации Swagger
- Middleware для Swagger UI на маршруте `/api-docs`
- Endpoint `/api-docs.json` для получения JSON спецификации

### 4. Документированы все API endpoints
**Файлы:** `src/routes/index.ts` и `src/routes/products.ts`

Добавлены JSDoc комментарии с описанием:
- **Health Check** - проверка статуса сервера
- **Products endpoints:**
  - GET /api/products - получить все продукты
  - GET /api/products/:id - получить продукт по ID
  - POST /api/products - создать продукт
  - PUT /api/products/:id - обновить продукт
  - DELETE /api/products/:id - удалить продукт

Каждый endpoint содержит:
- Описание функциональности
- Параметры запроса (query, path, body)
- Примеры запросов и ответов
- Возможные HTTP статус коды
- Описание ошибок

### 5. Обновлена документация
**Файлы:**
- `README.md` - добавлен раздел о Swagger
- `API_DOCUMENTATION.md` - создана подробная документация с примерами

## 🚀 Как использовать

### Запуск сервера

```bash
# С Docker (рекомендуется)
docker-compose up

# Или локально
npm run dev
```

### Доступ к документации

После запуска сервера откройте в браузере:

**Интерактивная документация:**
```
http://localhost:3000/api-docs
```

**JSON спецификация OpenAPI:**
```
http://localhost:3000/api-docs.json
```

## 📋 Структура Swagger конфигурации

```
src/config/swagger.ts
├── OpenAPI 3.0.0 определение
├── Информация о API
│   ├── Название: AssortiShop API
│   ├── Версия: 1.0.0
│   └── Описание
├── Серверы
│   ├── Development (localhost:3000)
│   └── Production (localhost:3001)
└── Компоненты (Schemas)
    ├── Category
    ├── Product
    ├── PaginatedProducts
    ├── SuccessResponse
    ├── ErrorResponse
    └── HealthCheck
```

## 🔌 Документированные endpoints

### Health Check
```
GET /api/health
```
Проверка статуса сервера

### Products
```
GET    /api/products              - Получить все продукты (с пагинацией)
GET    /api/products/:id          - Получить продукт по ID
POST   /api/products              - Создать новый продукт
PUT    /api/products/:id          - Обновить продукт
DELETE /api/products/:id          - Удалить продукт
```

## 📊 Примеры использования

### Через Swagger UI
1. Откройте http://localhost:3000/api-docs
2. Выберите нужный endpoint
3. Нажмите "Try it out"
4. Заполните параметры
5. Нажмите "Execute"

### Через curl
```bash
# Получить все продукты
curl http://localhost:3000/api/products

# Создать продукт
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Новый продукт",
    "price": 99.99,
    "categoryId": "cuid123abc"
  }'
```

### Через JavaScript
```javascript
// Получить все продукты
const response = await fetch('http://localhost:3000/api/products');
const data = await response.json();
console.log(data);
```

## 📁 Файлы, которые были изменены/созданы

### Созданы:
- ✅ `src/config/swagger.ts` - конфигурация Swagger
- ✅ `API_DOCUMENTATION.md` - подробная документация API
- ✅ `SWAGGER_SETUP.md` - этот файл

### Изменены:
- ✅ `src/app.ts` - добавлена интеграция Swagger
- ✅ `src/routes/index.ts` - добавлена документация для health check
- ✅ `src/routes/products.ts` - добавлена документация для всех endpoints
- ✅ `README.md` - добавлена информация о Swagger
- ✅ `package.json` - добавлены зависимости

## 🔍 Как добавить документацию для новых endpoints

1. **Создайте новый маршрут** в `src/routes/`

2. **Добавьте JSDoc комментарий** перед маршрутом:

```typescript
/**
 * @swagger
 * /api/new-endpoint:
 *   get:
 *     summary: Описание endpoint
 *     description: Подробное описание
 *     tags:
 *       - Category Name
 *     parameters:
 *       - in: query
 *         name: param_name
 *         schema:
 *           type: string
 *         description: Описание параметра
 *     responses:
 *       200:
 *         description: Успешный ответ
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/YourSchema'
 */
router.get('/new-endpoint', (req, res) => {
  // ваш код
});
```

3. **Добавьте схему данных** в `src/config/swagger.ts` (если нужна новая):

```typescript
YourSchema: {
  type: 'object',
  properties: {
    field1: {
      type: 'string',
      description: 'Описание поля'
    }
  }
}
```

## 🛠️ Полезные команды

```bash
# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск скомпилированного приложения
npm start

# Проверка кода
npm run lint

# Запуск миграций БД
npm run prisma:migrate

# Открыть Prisma Studio
npm run prisma:studio
```

## 📚 Дополнительные ресурсы

- [Swagger/OpenAPI документация](https://swagger.io/docs/)
- [swagger-jsdoc GitHub](https://github.com/Surnet/swagger-jsdoc)
- [swagger-ui-express GitHub](https://github.com/scottie1984/swagger-ui-express)
- [OpenAPI 3.0 Specification](https://spec.openapis.org/oas/v3.0.3)

## ✨ Особенности реализации

- ✅ Полная поддержка OpenAPI 3.0.0
- ✅ Интерактивный Swagger UI
- ✅ JSON спецификация для интеграции с другими инструментами
- ✅ Примеры запросов и ответов для каждого endpoint
- ✅ Описание всех параметров и типов данных
- ✅ Поддержка пагинации
- ✅ Документация ошибок и статус кодов
- ✅ Русскоязычная документация

## 🎯 Следующие шаги

Для полноты документации рекомендуется:

1. **Добавить endpoints для категорий** (Categories)
2. **Добавить endpoints для пользователей** (Users)
3. **Добавить endpoints для корзины** (Cart)
4. **Добавить аутентификацию** (JWT tokens)
5. **Добавить примеры ошибок** для каждого endpoint
6. **Создать Postman коллекцию** из OpenAPI спецификации

## 📝 Примечания

- Все документация написана на русском языке
- Примеры используют реальные данные из системы
- Swagger UI автоматически обновляется при изменении кода
- JSON спецификация может быть импортирована в Postman, Insomnia и другие инструменты

---

**Дата создания:** 12 февраля 2026 г.
**Версия:** 1.0.0
**Статус:** ✅ Готово к использованию
