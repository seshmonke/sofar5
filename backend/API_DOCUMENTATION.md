# 📚 API Документация AssortiShop

Полная документация по REST API бэкенда AssortiShop с примерами запросов и ответов.

## 🚀 Быстрый старт

### Доступ к интерактивной документации

После запуска сервера откройте в браузере:
```
http://localhost:3000/api-docs
```

Здесь вы сможете:
- 📖 Просмотреть все доступные endpoints
- 🧪 Тестировать API прямо из браузера
- 📋 Увидеть примеры запросов и ответов
- 🔍 Изучить структуру данных

### JSON спецификация OpenAPI

Полная спецификация доступна по адресу:
```
http://localhost:3000/api-docs.json
```

---

## 🏥 Health Check

### Проверка статуса сервера

**Endpoint:**
```
GET /api/health
```

**Описание:** Возвращает статус сервера и текущее время. Используется для проверки доступности API.

**Пример запроса:**
```bash
curl http://localhost:3000/api/health
```

**Успешный ответ (200 OK):**
```json
{
  "status": "OK",
  "timestamp": "2026-02-12T10:30:00.000Z"
}
```

---

## 📦 Products (Продукты)

### 1. Получить все продукты

**Endpoint:**
```
GET /api/products
```

**Описание:** Возвращает список всех продуктов с поддержкой пагинации.

**Query параметры:**
| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|---------|
| `page` | integer | 1 | Номер страницы |
| `limit` | integer | 10 | Количество элементов на странице (макс. 100) |

**Пример запроса:**
```bash
# Получить первую страницу с 10 элементами
curl "http://localhost:3000/api/products"

# Получить вторую страницу с 20 элементами
curl "http://localhost:3000/api/products?page=2&limit=20"
```

**Успешный ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "data": [
      {
        "id": "cuid456def",
        "name": "Смартфон Samsung Galaxy S21",
        "slug": "smartfon-samsung-galaxy-s21",
        "description": "Мощный смартфон с отличной камерой",
        "price": 49999.99,
        "image": "https://example.com/images/samsung-s21.jpg",
        "stock": 15,
        "categoryId": "cuid123abc",
        "category": {
          "id": "cuid123abc",
          "name": "Электроника",
          "slug": "elektronika",
          "description": "Электронные товары и гаджеты",
          "image": "https://example.com/images/electronics.jpg",
          "createdAt": "2026-02-12T10:00:00.000Z",
          "updatedAt": "2026-02-12T10:00:00.000Z"
        },
        "createdAt": "2026-02-12T10:15:00.000Z",
        "updatedAt": "2026-02-12T10:15:00.000Z"
      }
    ],
    "total": 50,
    "page": 1,
    "limit": 10,
    "totalPages": 5
  }
}
```

**Ошибка валидации (400 Bad Request):**
```json
{
  "success": false,
  "error": "Page и limit должны быть больше 0",
  "statusCode": 400
}
```

---

### 2. Получить продукт по ID

**Endpoint:**
```
GET /api/products/:id
```

**Описание:** Возвращает информацию о конкретном продукте.

**Path параметры:**
| Параметр | Тип | Описание |
|----------|-----|---------|
| `id` | string | ID продукта (обязательный) |

**Пример запроса:**
```bash
curl http://localhost:3000/api/products/cuid456def
```

**Успешный ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "id": "cuid456def",
    "name": "Смартфон Samsung Galaxy S21",
    "slug": "smartfon-samsung-galaxy-s21",
    "description": "Мощный смартфон с отличной камерой",
    "price": 49999.99,
    "image": "https://example.com/images/samsung-s21.jpg",
    "stock": 15,
    "categoryId": "cuid123abc",
    "category": {
      "id": "cuid123abc",
      "name": "Электроника",
      "slug": "elektronika",
      "description": "Электронные товары и гаджеты",
      "image": "https://example.com/images/electronics.jpg",
      "createdAt": "2026-02-12T10:00:00.000Z",
      "updatedAt": "2026-02-12T10:00:00.000Z"
    },
    "createdAt": "2026-02-12T10:15:00.000Z",
    "updatedAt": "2026-02-12T10:15:00.000Z"
  }
}
```

**Продукт не найден (404 Not Found):**
```json
{
  "success": false,
  "error": "Продукт с ID cuid456def не найден",
  "statusCode": 404
}
```

---

### 3. Создать новый продукт

**Endpoint:**
```
POST /api/products
```

**Описание:** Создает новый продукт в системе.

**Request Body:**
```json
{
  "name": "Смартфон Samsung Galaxy S21",
  "price": 49999.99,
  "categoryId": "cuid123abc",
  "description": "Мощный смартфон с отличной камерой",
  "image": "https://example.com/images/samsung-s21.jpg",
  "stock": 15
}
```

**Обязательные поля:**
- `name` (string) - Название продукта
- `price` (number) - Цена продукта
- `categoryId` (string) - ID категории

**Опциональные поля:**
- `description` (string) - Описание продукта
- `image` (string) - URL изображения
- `stock` (integer) - Количество на складе

**Пример запроса:**
```bash
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Смартфон Samsung Galaxy S21",
    "price": 49999.99,
    "categoryId": "cuid123abc",
    "description": "Мощный смартфон с отличной камерой",
    "image": "https://example.com/images/samsung-s21.jpg",
    "stock": 15
  }'
```

**Успешный ответ (201 Created):**
```json
{
  "success": true,
  "data": {
    "id": "cuid456def",
    "name": "Смартфон Samsung Galaxy S21",
    "slug": "smartfon-samsung-galaxy-s21",
    "description": "Мощный смартфон с отличной камерой",
    "price": 49999.99,
    "image": "https://example.com/images/samsung-s21.jpg",
    "stock": 15,
    "categoryId": "cuid123abc",
    "createdAt": "2026-02-12T10:15:00.000Z",
    "updatedAt": "2026-02-12T10:15:00.000Z"
  },
  "message": "Продукт успешно создан"
}
```

**Ошибка валидации (400 Bad Request):**
```json
{
  "success": false,
  "error": "Требуются поля: name, price, categoryId",
  "statusCode": 400
}
```

---

### 4. Обновить продукт

**Endpoint:**
```
PUT /api/products/:id
```

**Описание:** Обновляет информацию о продукте.

**Path параметры:**
| Параметр | Тип | Описание |
|----------|-----|---------|
| `id` | string | ID продукта (обязательный) |

**Request Body (все поля опциональны):**
```json
{
  "name": "Обновленное название",
  "price": 59999.99,
  "description": "Обновленное описание",
  "image": "https://example.com/images/new-image.jpg",
  "stock": 20
}
```

**Пример запроса:**
```bash
curl -X PUT http://localhost:3000/api/products/cuid456def \
  -H "Content-Type: application/json" \
  -d '{
    "price": 59999.99,
    "stock": 20
  }'
```

**Успешный ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "id": "cuid456def",
    "name": "Смартфон Samsung Galaxy S21",
    "slug": "smartfon-samsung-galaxy-s21",
    "description": "Мощный смартфон с отличной камерой",
    "price": 59999.99,
    "image": "https://example.com/images/samsung-s21.jpg",
    "stock": 20,
    "categoryId": "cuid123abc",
    "createdAt": "2026-02-12T10:15:00.000Z",
    "updatedAt": "2026-02-12T10:20:00.000Z"
  },
  "message": "Продукт успешно обновлен"
}
```

**Продукт не найден (404 Not Found):**
```json
{
  "success": false,
  "error": "Продукт с ID cuid456def не найден",
  "statusCode": 404
}
```

---

### 5. Удалить продукт

**Endpoint:**
```
DELETE /api/products/:id
```

**Описание:** Удаляет продукт из системы.

**Path параметры:**
| Параметр | Тип | Описание |
|----------|-----|---------|
| `id` | string | ID продукта (обязательный) |

**Пример запроса:**
```bash
curl -X DELETE http://localhost:3000/api/products/cuid456def
```

**Успешный ответ (200 OK):**
```json
{
  "success": true,
  "message": "Продукт успешно удален"
}
```

**Продукт не найден (404 Not Found):**
```json
{
  "success": false,
  "error": "Продукт с ID cuid456def не найден",
  "statusCode": 404
}
```

---

## 📊 Структуры данных

### Product (Продукт)

```typescript
{
  id: string;              // Уникальный идентификатор (CUID)
  name: string;            // Название продукта
  slug: string;            // URL-friendly название
  description?: string;    // Описание продукта
  price: number;           // Цена продукта
  image?: string;          // URL изображения
  stock: number;           // Количество на складе
  categoryId: string;      // ID категории
  category?: Category;     // Объект категории
  createdAt: DateTime;     // Дата создания
  updatedAt: DateTime;     // Дата последнего обновления
}
```

### Category (Категория)

```typescript
{
  id: string;              // Уникальный идентификатор (CUID)
  name: string;            // Название категории
  slug: string;            // URL-friendly название
  description?: string;    // Описание категории
  image?: string;          // URL изображения категории
  createdAt: DateTime;     // Дата создания
  updatedAt: DateTime;     // Дата последнего обновления
}
```

### PaginatedResponse (Пагинированный ответ)

```typescript
{
  data: Product[];         // Массив продуктов
  total: number;           // Общее количество продуктов
  page: number;            // Текущая страница
  limit: number;           // Количество элементов на странице
  totalPages: number;      // Общее количество страниц
}
```

---

## 🔍 HTTP Статус коды

| Код | Описание |
|-----|---------|
| 200 | OK - Успешный запрос |
| 201 | Created - Ресурс успешно создан |
| 400 | Bad Request - Ошибка валидации |
| 404 | Not Found - Ресурс не найден |
| 500 | Internal Server Error - Ошибка сервера |

---

## 🛠️ Примеры использования

### JavaScript/TypeScript (Fetch API)

```typescript
// Получить все продукты
const response = await fetch('http://localhost:3000/api/products?page=1&limit=10');
const data = await response.json();
console.log(data);

// Создать продукт
const newProduct = await fetch('http://localhost:3000/api/products', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'Новый продукт',
    price: 99.99,
    categoryId: 'cuid123abc',
    stock: 10
  })
});
const created = await newProduct.json();
console.log(created);

// Обновить продукт
const updated = await fetch('http://localhost:3000/api/products/cuid456def', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ price: 149.99 })
});
const result = await updated.json();
console.log(result);

// Удалить продукт
const deleted = await fetch('http://localhost:3000/api/products/cuid456def', {
  method: 'DELETE'
});
const deleteResult = await deleted.json();
console.log(deleteResult);
```

### Python (Requests)

```python
import requests

BASE_URL = 'http://localhost:3000/api'

# Получить все продукты
response = requests.get(f'{BASE_URL}/products', params={'page': 1, 'limit': 10})
print(response.json())

# Создать продукт
new_product = {
    'name': 'Новый продукт',
    'price': 99.99,
    'categoryId': 'cuid123abc',
    'stock': 10
}
response = requests.post(f'{BASE_URL}/products', json=new_product)
print(response.json())

# Обновить продукт
updated_data = {'price': 149.99}
response = requests.put(f'{BASE_URL}/products/cuid456def', json=updated_data)
print(response.json())

# Удалить продукт
response = requests.delete(f'{BASE_URL}/products/cuid456def')
print(response.json())
```

---

## 📝 Примечания

- Все даты возвращаются в формате ISO 8601 (UTC)
- ID продуктов генерируются автоматически (CUID)
- Slug генерируется автоматически из названия
- Максимальное количество элементов на странице: 100
- Все запросы должны содержать заголовок `Content-Type: application/json`

---

## 🔗 Полезные ссылки

- [Swagger UI](http://localhost:3000/api-docs) - Интерактивная документация
- [OpenAPI Spec](http://localhost:3000/api-docs.json) - JSON спецификация
- [Express.js документация](https://expressjs.com/)
- [Prisma документация](https://www.prisma.io/docs/)

---

## ❓ Часто задаваемые вопросы

**Q: Как получить продукты определенной категории?**
A: Используйте параметр `categoryId` при фильтрации (функция будет добавлена в будущих версиях).

**Q: Какой максимальный размер страницы?**
A: Максимум 100 элементов на странице. Если указать больше, будет использовано значение 100.

**Q: Как работает пагинация?**
A: Используйте параметры `page` (номер страницы, начиная с 1) и `limit` (количество элементов).

**Q: Можно ли обновить только некоторые поля?**
A: Да, при PUT запросе можно передать только те поля, которые нужно обновить.

---

**Последнее обновление:** 12 февраля 2026 г.
