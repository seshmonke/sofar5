# Инструкция по настройке Telegram бота для управления товарами

Этот документ содержит полные инструкции по настройке и запуску Telegram бота для управления товарами интернет-магазина с поддержкой QR-кодов.

## 📋 Содержание

1. [Требования](#требования)
2. [Получение Telegram Bot Token](#получение-telegram-bot-token)
3. [Настройка Backend](#настройка-backend)
4. [Настройка Telegram бота](#настройка-telegram-бота)
5. [Запуск приложения](#запуск-приложения)
6. [Использов��ние бота](#использование-бота)

## 🔧 Требования

- Node.js 16+
- npm или yarn
- PostgreSQL база данных
- Telegram аккаунт

## 🤖 Получение Telegram Bot Token

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "AssortiShop Admin Bot")
   - Введите username бота (должен заканчиваться на `_bot`, например: `assorti_shop_admin_bot`)
4. Скопируйте полученный токен (выглядит как: `123456789:ABCdefGHIjklmnoPQRstuvWXYZ`)

## 🔌 Настройка Backend

### 1. Обновление Prisma схемы

Prisma схема уже обновлена с новыми моделями `Sale` и `QRCode`. Нужно создать миграцию:

```bash
cd backend
npx prisma migrate dev --name add_sales_and_qrcodes
```

Это создаст новые таблицы в базе данных.

### 2. Генерация Prisma клиента

```bash
npx prisma generate
```

### 3. Запуск Backend сервера

```bash
npm run dev
```

Backend будет доступен на `http://localhost:3000`

## 🤖 Настройка Telegram бота

### 1. Установка зависимостей

```bash
cd telegram-admin-bot
npm install
```

### 2. Создание файла .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

### 3. Заполнение переменных окружения

Отредактируйте файл `.env`:

```env
# Telegram Bot Token (п��лученный от @BotFather)
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ

# Backend API URL
BACKEND_API_URL=http://localhost:3000/api

# Ваш Telegram ID (для администрирования)
# Получить ID: отправьте /start боту @userinfobot
ADMIN_USER_ID=123456789

# Окружение
NODE_ENV=development
```

### 4. Запуск бота

```bash
npm run dev
```

Бот будет запущен и готов к использованию.

## 🚀 Запуск приложения

### Вариант 1: Запуск всех компонентов отдельно

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Telegram бот:**
```bash
cd telegram-admin-bot
npm run dev
```

**Терминал 3 - Frontend (опционально):**
```bash
cd my-app
npm run dev
```

### Вариант 2: Использование Docker (если настроено)

```bash
docker-compose up
```

## 📱 Использование бота

### Запуск бота

1. Откройте Telegram
2. Найдите вашего бота по username (например: `@assorti_shop_admin_bot`)
3. Отправьте команду `/start`

### Основные команды

- `/start` - Начать работу с ботом
- `/help` - Справка по командам

### Кнопки меню

После команды `/start` вы увидите кнопки:

- **Все товары** - Показать список всех товаров
- **Добавить товары** - Добавить новый товар
- **Редактировать** - Редактировать товар
- **Продажа** - Продать товар (сканирование QR)
- **Категории** - Управление категориями
- **Генерировать QR** - Создать QR-коды для печати

## 📖 Примеры использования

### Добавление товара

1. Нажмите кнопку "Добавить товары"
2. Введите название товара (например: "Ноутбук Dell")
3. Введите описание или пропустите (-)
4. Введите цену (��апример: 50000)
5. Введите количество на складе (например: 10)
6. Выберите категорию из списка
7. Товар добавлен!

### Продажа товара

1. Нажмите кнопку "Продажа"
2. Отправьте QR-код товара или введите его ID
3. Введите количество для продажи
4. Продажа зарегистрирована!

### Генерация QR-кодов

1. Нажмите кнопку "Генерировать QR"
2. Выберите товар из списка
3. Получите QR-код для печати на термопринтере

## 🔌 API Endpoints

### Товары
- `GET /api/products` - Получить все товары
- `GET /api/products/:id` - Получить товар по ID
- `GET /api/products/barcode/:barcode` - Получить товар по штрих-коду
- `POST /api/products` - Создать товар
- `PUT /api/products/:id` - Обновить товар
- `PUT /api/products/:id/stock` - Обновить количество товара
- `DELETE /api/products/:id` - Удалить товар

### Категории
- `GET /api/categories` - Получить все категории
- `GET /api/categories/:id` - Получить категорию по ID
- `POST /api/categories` - Создать категорию
- `PUT /api/categories/:id` - Обновить категорию
- `DELETE /api/categories/:id` - Удалить категорию

### Продажи
- `GET /api/sales` - Получить все продажи
- `GET /api/sales/:id` - Получить продажу по ID
- `POST /api/sales` - Создать продажу

### QR-коды
- `GET /api/qrcodes` - Получить все QR-коды
- `GET /api/qrcodes/:productId` - Получить QR-код товара
- `POST /api/qrcodes/generate` - Генерировать QR-код

## 🐛 Решение проб��ем

### Бот не отвечает

1. Проверьте, что Backend запущен на `http://localhost:3000`
2. Проверьте переменные окружения в `.env`
3. Проверьте логи бота �� консоли

### Ошибка подключения к базе данных

1. Убедитесь, что PostgreSQL запущена
2. Проверьте переменную `DATABASE_URL` в backend `.env`
3. Запустите миграции: `npx prisma migrate dev`

### QR-коды не генерируются

1. Убедитесь, что товар существует в базе данных
2. Проверьте логи backend сервера
3. Убедитесь, что библиотека `qrcode` установлена

## 📝 Логирование

Оба приложения (backend и бот) логируют все операции:

- **Backend**: логи выводятся в консоль с вр��менными метками
- **Бот**: логи выводятся в консоль с уровнями (info, error, warn, debug)

Для включения debug логов установите `NODE_ENV=development` в `.env`

## 🔐 Безопасность

- Никогда не коммитьте файл `.env` в git
- Используйте сильные пароли для базы данных
- Ограничьте доступ к API endpoints при необходимости
- Регулярно обновляйте зависимости

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи в консоли
2. Убедитесь, что все сервисы запущены
3. Проверьте переменные окружения
4. Перезагрузите приложение

## 📄 Лицензия

ISC

---

**Создано для асортиментного магазина**
