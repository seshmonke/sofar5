# 🚀 Быстрый старт Telegram бота

## ✅ Статус: Все готово к запуску!

Все файлы созданы, зависимости установлены, база данных мигрирована. Вам осталось только запустить приложение!

## 📋 Что было сделано

- ✅ Создана полная структура Telegram бота на TypeScript с grammy.js
- ✅ Расширен Backend API (новые endpoints для категорий, продаж, QR-кодов)
- ✅ Обновлена Prisma схема (добавлены модели Sale и QRCode)
- ✅ Создана миграция базы данных
- ✅ Установлены все зависимости
- ✅ Docker PostgreSQL запущена и готова

## 🎯 Следующие шаги

### 1️⃣ Получить Telegram Bot Token

Если у вас еще нет токена:

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "AssortiShop Admin Bot")
   - Введите username бота (должен заканчи��аться на `_bot`, например: `assorti_shop_admin_bot`)
4. Скопируйте полученный токен

### 2️⃣ Настроить переменные окружения

Создайте файл `.env` в папке `telegram-admin-bot`:

```bash
cd telegram-admin-bot
cp .env.example .env
```

Отредактируйте `.env` и доб��вьте ваш токен:

```env
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
BACKEND_API_URL=http://localhost:3000/api
ADMIN_USER_ID=123456789
NODE_ENV=development
```

**Где найти ADMIN_USER_ID:**
- Отправьте `/start` боту @userinfobot в Telegram
- Скопируйте ваш ID

### 3️⃣ Запустить Backend

**Терминал 1:**
```bash
cd backend
npm run dev
```

Backend будет доступен на `http://localhost:3000`

### 4️⃣ Запустить Telegram бота

**Терминал 2:**
```bash
cd telegram-admin-bot
npm run dev
```

Бот будет запущен и готов к использованию!

### 5️⃣ Использовать бота

1. Откройте Telegram
2. Найдите вашего бота по username (например: `@assorti_shop_admin_bot`)
3. Отправьте команду `/start`
4. Используйте кнопки меню для управления товарами

## 📊 Функциональность

После запуска бота вы сможете:

- 📦 **Просмотреть все товары** - показать список товаров с пагинацией
- ➕ **Добавить товары** - создать новый товар с названием, описанием, ценой, количеством
- ✏️ **Редактировать** - изменить параметры товара
- 💰 **Продажа** - зарегистрировать продажу товара через QR-код
- 📂 **Категории** - управлять категориями товаров
- 🔲 **Генерировать QR** - создать QR-коды для печати на термопринтере

## 🔌 API Endpoints

В��е endpoints доступны на `http://localhost:3000/api`:

### Товары
- `GET /products` - все товары
- `GET /products/:id` - товар по ID
- `GET /products/barcode/:barcode` - товар по штрих-коду
- `POST /products` - создать товар
- `PUT /products/:id` - обновить товар
- `PUT /products/:id/stock` - обновить количество
- `DELETE /products/:id` - удалить товар

### Категории
- `GET /categories` - все категории
- `POST /categories` - создать категорию
- `PUT /categories/:id` - обновить категорию
- `DELETE /categories/:id` - удалить категорию

### Продажи
- `GET /sales` - все продажи
- `POST /sales` - создать продажу

### QR-коды
- `GET /qrcodes` - все QR-коды
- `POST /qrcodes/generate` - генерировать QR-код

## 📁 Структура проекта

```
telegram-admin-bot/
├── src/
│   ├── main.ts                 # Точка входа
│   ├── bot.ts                  # Инициализация бота
│   ├── config/                 # Конфигурация
│   ├── handlers/               # Обработчики команд (6 файлов)
│   ├── services/               # Бизнес-логика (3 файла)
│   ├── middleware/             # Middleware логирования
│   ├── types/                  # TypeScript типы
│   └── utils/                  # Утилиты
├── package.json                # Зависимости
├── tsconfig.json               # Конфигурация TypeScript
└── .env                        # Переменные окружения

backend/
├── src/
│   ├── controllers/            # 3 новых контроллера
│   ├── routes/                 # 3 новых маршрута
│   ├── services/               # Обновленные сервисы
│   └── repositories/           # Обновленные репозитории
└── prisma/
    ├── schema.prisma           # Обновленная схема
    └── migrations/             # Миграции БД
```

## 🐛 Решение проблем

### Бот не отвечает
1. Проверьте, что Backend запущен на `http://localhost:3000`
2. Проверьте переменные окружения в `.env`
3. Проверьте логи в консоли

### Ошибка подключения к БД
1. Убедитесь, что Docker контейнеры запущены: `docker ps`
2. Если нет, запустите: `cd backend && docker-compose up -d`

### QR-коды не генерируются
1. Убедитесь, что товар существует в БД
2. Проверьте логи backend сервера

## 📝 Логирование

Оба приложения логируют все операции:

- **Backend**: логи в консоль с временными метками
- **Бот**: логи в консоль с уровнями (info, error, warn, debug)

## 🔐 Безопасность

- ✅ Переменные окружения в `.env` (не коммитьте в git!)
- ✅ Валидация входных данных
- ✅ Обработка ошибок API
- ✅ Логирование операций

## 📚 Документация

- **TELEGRAM_BOT_SETUP.md** - полная инструкция по настройке
- **TELEGRAM_BOT_CREATED_FILES.md** - описание всех файлов
- **TELEGRAM_BOT_SUMMARY.md** - резюме проекта
- **telegram-admin-bot/README.md** - документация бота

## 🎉 Готово!

Все готово к запуску! Следуйте инструкциям выше и начните использовать бота.

**Вопросы?** Проверьте документацию или логи в консоли.

---

**Создано:** 18 февраля 2026
**Версия:** 1.0.0
**Статус:** ✅ Готово к использованию
