# 🐳 Docker - Полное руководство

## Содержание
1. [Что такое Docker](#что-такое-docker)
2. [Основные концепции](#основные-концепции)
3. [Dockerfile](#dockerfile)
4. [Docker Compose](#docker-compose)
5. [Как работает ваше приложение в Docker](#как-работает-ваше-приложение-в-docker)
6. [Команды Docker](#команды-docker)
7. [Практические примеры](#практические-примеры)

---

## Что такое Docker

### 📖 Определение

**Docker** - это платформа для контейнеризации приложений. Контейнер - это изолированная среда, которая содержит:
- Приложение
- Зависимости
- Библиотеки
- Конфигурация

### 🎯 Зачем нужен Docker

**Проблема без Docker:**
```
Разработчик: "У меня работает!"
Тестировщик: "У меня не работает!"
DevOps: "Это не моя проблема!"
```

**С Docker:**
```
Разработчик: "Вот контейнер, он работает везде!"
Тестировщик: "Отлично, работает!"
DevOps: "Отлично, развернул на сервере!"
```

### ✨ Преимущества Docker

- **Консистентность** - одинаковое окружение везде
- **Изоляция** - приложения не мешают друг другу
- **Масштабируемость** - легко запустить несколько копий
- **Простота** - легко развернуть на любой машине
- **Скорость** - быстрый запуск

---

## Основные концепции

### 1️⃣ Image (Образ)

**Что это:** Шаблон для создания контейнеров

**Аналогия:** Как чертеж дома

```bash
# Посмотреть образы
docker images

# Удалить образ
docker rmi image_name

# Скачать образ
docker pull postgres:16-alpine
```

### 2️⃣ Container (Контейнер)

**Что это:** Запущенный экземпляр образа

**Аналогия:** Как построенный дом

```bash
# Посмотреть контейнеры
docker ps          # Только запущенные
docker ps -a       # Все контейнеры

# Запустить контейнер
docker run -d --name my_container postgres:16-alpine

# Остановить контейнер
docker stop container_name

# Удалить контейнер
docker rm container_name
```

### 3️⃣ Registry (Реестр)

**Что это:** Хранилище образов

**Примеры:**
- Docker Hub (официальный)
- GitHub Container Registry
- AWS ECR

```bash
# Скачать образ из реестра
docker pull postgres:16-alpine

# Загрузить образ в реестр
docker push username/image_name:tag
```

### 4️⃣ Volume (Том)

**Что это:** Хранилище данных, которое сохраняется после удаления контейнера

**Аналогия:** Как внешний жесткий диск

```bash
# Создать том
docker volume create my_volume

# Посмотреть тома
docker volume ls

# Удалить том
docker volume rm my_volume
```

### 5️⃣ Network (Сеть)

**Что это:** Сеть для общения между контейнерами

```bash
# Создать сеть
docker network create my_network

# Посмотреть сети
docker network ls

# Удалить сеть
docker network rm my_network
```

---

## Dockerfile

### 📖 Что такое Dockerfile

**Dockerfile** - это файл с инструкциями для создания образа. Файл: `backend/docker/Dockerfile`

### 💻 Структура Dockerfile

```dockerfile
# Базовый образ
FROM node:20-alpine

# Рабочая директория
WORKDIR /app

# Установка зависимостей системы
RUN apk add --no-cache openssl

# Копирование файлов
COPY package*.json ./

# Установка зависимостей npm
RUN npm install

# Копирование кода
COPY . .

# Генерирование Prisma клиента
RUN npm run prisma:generate

# Сборка TypeScript
RUN npm run build

# Открытие порта
EXPOSE 3000

# Команда запуска
CMD ["npm", "start"]
```

### 🔍 Инструкции Dockerfile

| Инструкция | Назначение | Пример |
|-----------|-----------|--------|
| `FROM` | Базовый образ | `FROM node:20-alpine` |
| `WORKDIR` | Рабочая директория | `WORKDIR /app` |
| `RUN` | Выполнить команду | `RUN npm install` |
| `COPY` | Копировать файлы | `COPY . .` |
| `ADD` | Копировать файлы (с распаковкой) | `ADD archive.tar.gz .` |
| `ENV` | Переменная окружения | `ENV NODE_ENV=production` |
| `EXPOSE` | Открыть порт | `EXPOSE 3000` |
| `CMD` | Команда по умолчанию | `CMD ["npm", "start"]` |
| `ENTRYPOINT` | Точка входа | `ENTRYPOINT ["node"]` |
| `VOLUME` | Том | `VOLUME ["/data"]` |

### 📊 Жизненный цикл Dockerfile

```
1. FROM node:20-alpine
   ↓ Скачивает базовый образ Node.js
   
2. WORKDIR /app
   ↓ Создает рабочую директорию
   
3. RUN apk add --no-cache openssl
   ↓ Устанавливает OpenSSL
   
4. COPY package*.json ./
   ↓ Копирует package.json и package-lock.json
   
5. RUN npm install
   ↓ Устанавливает зависимости npm
   
6. COPY . .
   ↓ Копирует весь код
   
7. RUN npm run prisma:generate
   ↓ Генерирует Prisma клиент
   
8. RUN npm run build
   ↓ Собирает TypeScript
   
9. EXPOSE 3000
   ↓ Открывает порт 3000
   
10. CMD ["npm", "start"]
    ↓ Команда запуска контейнера
```

### 🎯 Ваш Dockerfile

```dockerfile
# backend/docker/Dockerfile

FROM node:20-alpine
# Используем Node.js 20 на Alpine Linux (маленький образ)

WORKDIR /app
# Устанавливаем рабочую директорию

RUN apk add --no-cache openssl
# Устанавливаем OpenSSL (нужен для Prisma)

COPY package*.json ./
# Копируем package.json и package-lock.json

RUN npm install
# Устанавливаем зависимости

COPY . .
# Копируем весь код

RUN npm run prisma:generate
# Генерируем Prisma клиент

RUN npm run build
# Собираем TypeScript в JavaScript

EXPOSE 3000
# Открываем порт 3000

CMD ["npm", "start"]
# Запускаем приложение
```

---

## Docker Compose

### 📖 Что такое Docker Compose

**Docker Compose** - это инструмент для определения и запуска многоконтейнерных приложений. Файл: `backend/docker-compose.yml`

### 💻 Структура Docker Compose

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: assortiShop_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: assortiShop
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - assortiShop_network

  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: assortiShop_backend
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/assortiShop
      NODE_ENV: development
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    networks:
      - assortiShop_network

volumes:
  postgres_data:

networks:
  assortiShop_network:
    driver: bridge
```

### 🔍 Ключевые секции

#### 1. Services (Сервисы)

```yaml
services:
  postgres:      # Имя сервиса
    image: ...   # Образ
    ports: ...   # Порты
    environment: # Переменные окружения
    volumes: ...  # Тома
```

#### 2. Ports (Порты)

```yaml
ports:
  - "5432:5432"  # host_port:container_port
  - "3000:3000"
```

**Что это значит:**
- Порт 5432 на хосте → Порт 5432 в контейнере
- Порт 3000 на хосте → Порт 3000 в контейнере

#### 3. Environment (Переменные окружения)

```yaml
environment:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  DATABASE_URL: postgresql://postgres:postgres@postgres:5432/assortiShop
```

#### 4. Volumes (Тома)

```yaml
volumes:
  - postgres_data:/var/lib/postgresql/data
  # named_volume:container_path
  
  - .:/app
  # host_path:container_path
  
  - /app/node_modules
  # container_path (анонимный том)
```

#### 5. Depends On (Зависимости)

```yaml
depends_on:
  postgres:
    condition: service_healthy
# Backend ждет, пока PostgreSQL будет здоров
```

#### 6. Healthcheck (Проверка здоровья)

```yaml
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U postgres"]
  interval: 10s      # Проверять каждые 10 секунд
  timeout: 5s        # Таймаут 5 секунд
  retries: 5         # Попыток 5
```

#### 7. Networks (Сети)

```yaml
networks:
  assortiShop_network:
    driver: bridge
```

**Зачем нужны:**
- Контейнеры могут общаться по имени
- `postgres` может обращаться к `backend` как `http://backend:3000`

---

## Как работает ваше приложение в Docker

### 📊 Архитектура Docker Compose

```
┌─────────────────────────────────────────────────────┐
│           Docker Compose Network                    │
│  (assortiShop_network)                              │
│                                                     │
│  ┌──────────────────┐      ┌──────────────────┐   │
│  │   PostgreSQL     │      │   Backend        │   │
│  │   Container      │◄────►│   Container      │   │
│  │                  │      │                  │   │
│  │ Port: 5432       │      │ Port: 3000       │   │
│  │ Volume: postgres │      │ Volume: /app     │   │
│  │        _data     │      │                  │   │
│  └──────────────────┘      └──────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
         ↓                              ↓
    Host Machine                   Host Machine
    Port 5432                      Port 3000
```

### 🔄 Жизненный цикл запуска

```
1. docker-compose up
   ↓
2. Docker создает сеть assortiShop_network
   ↓
3. Docker создает том postgres_data
   ↓
4. Docker запускает PostgreSQL контейнер
   ↓
5. Docker проверяет здоровье PostgreSQL (healthcheck)
   ↓
6. PostgreSQL готов
   ↓
7. Docker собирает образ Backend (docker build)
   ↓
8. Docker запускает Backend контейнер
   ↓
9. Backend подключается к PostgreSQL
   ↓
10. Приложение готово к работе
```

### 🔗 Как контейнеры общаются

```
Backend контейнер хочет подключиться к PostgreSQL

DATABASE_URL = "postgresql://postgres:postgres@postgres:5432/assortiShop"
                                        ↑
                                   Имя сервиса!

Docker Compose преобразует "postgres" в IP адрес контейнера PostgreSQL

Backend → Docker DNS → PostgreSQL IP → PostgreSQL контейнер
```

---

## Команды Docker

### 🔧 Основные команды

```bash
# Запустить контейнеры
docker-compose up

# Запустить в фоне
docker-compose up -d

# Остановить контейнеры
docker-compose down

# Посмотреть логи
docker-compose logs

# Посмотреть логи конкретного сервиса
docker-compose logs backend

# Посмотреть логи в реальном времени
docker-compose logs -f backend

# Выполнить команду в контейнере
docker-compose exec backend npm run build

# Перестроить образ
docker-compose build

# Перестроить и запустить
docker-compose up --build

# Удалить контейнеры и тома
docker-compose down -v
```

### 📋 Полезные команды

```bash
# Посмотреть статус контейнеров
docker-compose ps

# Посмотреть использование ресурсов
docker stats

# Войти в контейнер
docker-compose exec backend bash

# Посмотреть переменные окружения
docker-compose exec backend env

# Перезагрузить контейнер
docker-compose restart backend

# Пересоздать контейнер
docker-compose up -d --force-recreate backend
```

---

## Практические примеры

### Пример 1: Запустить приложение

```bash
# Перейти в директорию backend
cd backend

# Запустить контейнеры
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f backend
```

**Результат:**
```
CONTAINER ID   IMAGE                    STATUS
abc123         assortiShop_backend      Up 2 minutes
def456         postgres:16-alpine       Up 2 minutes
```

### Пример 2: Посмотреть логи

```bash
# Логи всех контейнеров
docker-compose logs

# Логи только backend
docker-compose logs backend

# Логи в реальном времени
docker-compose logs -f backend

# Последние 100 строк
docker-compose logs --tail=100 backend
```

### Пример 3: Выполнить команду в контейнере

```bash
# Запустить миграции
docker-compose exec backend npx prisma migrate deploy

# Сгенерировать Prisma клиент
docker-compose exec backend npx prisma generate

# Запустить npm скрипт
docker-compose exec backend npm run build

# Войти в bash
docker-compose exec backend bash
```

### Пример 4: Остановить и удалить

```bash
# Остановить контейнеры
docker-compose stop

# Удалить контейнеры
docker-compose down

# Удалить контейнеры и тома (ОСТОРОЖНО!)
docker-compose down -v
```

### Пример 5: Перестроить образ

```bash
# Перестроить образ backend
docker-compose build backend

# Перестроить и запустить
docker-compose up --build -d

# Перестроить без кэша
docker-compose build --no-cache backend
```

---

## Диаграммы

### Слои Docker образа

```
┌─────────────────────────────────┐
│  CMD ["npm", "start"]           │ Layer 10
├─────────────────────────────────┤
│  EXPOSE 3000                    │ Layer 9
├─────────────────────────────────┤
│  RUN npm run build              │ Layer 8
├─────────────────────────────────┤
│  RUN npm run prisma:generate    │ Layer 7
├─────────────────────────────────┤
│  COPY . .                       │ Layer 6
├─────────────────────────────────┤
│  RUN npm install                │ Layer 5
├─────────────────────────────────┤
│  COPY package*.json ./          │ Layer 4
├─────────────────────────────────┤
│  RUN apk add --no-cache openssl │ Layer 3
├─────────────────────────────────┤
│  WORKDIR /app                   │ Layer 2
├─────────────────────────────────┤
│  FROM node:20-alpine            │ Layer 1
└─────────────────────────────────┘
```

### Процесс сборки образа

```
Dockerfile
    ↓
Docker читает инструкции
    ↓
Для каждой инструкции создается слой
    ↓
Слои складываются в стек
    ↓
Создается образ
    ↓
Образ можно запустить как контейнер
```

### Жизненный цикл контейнера

```
docker-compose up
    ↓
Создание контейнера
    ↓
Запуск контейнера
    ↓
Выполнение CMD
    ↓
Контейнер работает
    ↓
docker-compose down
    ↓
Остановка контейнера
    ↓
Удаление контейнера
```

---

## Ключевые моменты

✅ Docker - это контейнеризация приложений
✅ Dockerfile - инструкции для создания образа
✅ Docker Compose - управление многоконтейнерными приложениями
✅ Контейнеры изолированы и независимы
✅ Тома сохраняют данные
✅ Сети позволяют контейнерам общаться
✅ Healthcheck проверяет готовность сервиса

---

## Следующие шаги

1. Прочитайте **LAYERED_ARCHITECTURE_EXPLAINED.md** - разберитесь с каждым слоем
2. Прочитайте **DATA_FLOW_BACKEND.md** - проследите полный путь запроса
3. Прочитайте **PRACTICAL_EXAMPLES.md** - практикуйтесь с примерами
