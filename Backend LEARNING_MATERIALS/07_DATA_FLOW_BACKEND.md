# 🔄 Полный поток данных в бэкенде

## Содержание
1. [Обзор потока данных](#обзор-потока-данных)
2. [Пример 1: GET запрос](#пример-1-get-запрос)
3. [Пример 2: POST запрос](#пример-2-post-запрос)
4. [Пример 3: Обработка ошибок](#пример-3-обработка-ошибок)
5. [Диаграммы потока](#диаграммы-потока)

---

## Обзор потока данных

### 📊 Общая схема

```
КЛИЕНТ (Фронтенд)
    ↓ HTTP запрос
ROUTES слой
    ↓ Передача управления
MIDDLEWARE (CORS, JSON парсинг)
    ↓ Обработка запроса
CONTROLLERS слой
    ↓ Получение параметров
SERVICES слой
    ↓ Валидация и бизнес-логика
REPOSITORIES слой
    ↓ Работа с БД
DATABASE (PostgreSQL)
    ↓ Возврат данных
REPOSITORIES слой
    ↓ Преобразование данных
SERVICES слой
    ↓ Трансформация результата
CONTROLLERS слой
    ↓ Формирование JSON ответа
ROUTES слой
    ↓ Отправка ответа
КЛИЕНТ (Фронтенд)
    ↓ HTTP ответ
```

---

## Пример 1: GET запрос

### 📋 Задача: Получить все товары с пагинацией

**HTTP Запрос:**
```
GET /api/products?page=1&limit=10
Host: localhost:3000
Content-Type: application/json
```

### 🔄 Пошаговый поток

#### Шаг 1: Клиент отправляет запрос

```typescript
// Фронтенд (React)
const response = await fetch('http://localhost:3000/api/products?page=1&limit=10');
const data = await response.json();
```

#### Шаг 2: Express получает запрос

```
GET /api/products?page=1&limit=10
```

#### Шаг 3: Middleware обрабатывают запрос

```typescript
// src/app.ts
app.use(express.json());           // Парсит JSON (если есть)
app.use(corsMiddleware);           // Проверяет CORS
app.use(loggingMiddleware);        // Логирует запрос
```

**Логи:**
```
[INFO] GET /api/products
```

#### Шаг 4: Router находит маршрут

```typescript
// src/routes/products.ts
router.get('/', (req, res, next) => 
  productController.getAllProducts(req, res, next)
);
```

#### Шаг 5: Controller обрабатывает запрос

```typescript
// src/controllers/productController.ts
async getAllProducts(req: Request, res: Response, next: NextFunction) {
  try {
    // 5.1 Получение параметров
    const page = parseInt(req.query.page as string) || 1;
    const limit = parseInt(req.query.limit as string) || 10;
    
    console.log('Controller: page =', page, 'limit =', limit);
    
    // 5.2 Вызов сервиса
    const result = await productService.getAllProducts(page, limit);
    
    console.log('Controller: received result from service');
    
    // 5.3 Формирование ответа
    res.status(200).json({
      success: true,
      data: result,
    });
  } catch (error) {
    next(error);
  }
}
```

**Логи:**
```
Controller: page = 1 limit = 10
```

#### Шаг 6: Service выполняет бизнес-логику

```typescript
// src/services/productService.ts
async getAllProducts(page: number = 1, limit: number = 10) {
  // 6.1 Валидация
  if (page < 1 || limit < 1) {
    throw new ValidationError('Page и limit должны быть больше 0');
  }
  
  if (limit > 100) {
    limit = 100;
  }
  
  console.log('Service: validating parameters');
  
  // 6.2 Бизнес-логика
  const skip = (page - 1) * limit;
  
  console.log('Service: skip =', skip, 'limit =', limit);
  
  // 6.3 Вызов репозитория
  const [products, total] = await Promise.all([
    productRepository.findAll(skip, limit),
    productRepository.count(),
  ]);
  
  console.log('Service: received', products.length, 'products from repository');
  
  // 6.4 Трансформация данных
  return {
    data: products,
    total,
    page,
    limit,
    totalPages: Math.ceil(total / limit),
  };
}
```

**Логи:**
```
Service: validating parameters
Service: skip = 0 limit = 10
Service: received 10 products from repository
```

#### Шаг 7: Repository работает с БД

```typescript
// src/repositories/productRepository.ts
async findAll(skip: number = 0, take: number = 10) {
  console.log('Repository: querying database with skip =', skip, 'take =', take);
  
  const products = await prisma.product.findMany({
    skip,
    take,
    include: {
      category: true,
    },
  });
  
  console.log('Repository: found', products.length, 'products');
  
  return products;
}

async count() {
  console.log('Repository: counting all products');
  
  const count = await prisma.product.count();
  
  console.log('Repository: total products =', count);
  
  return count;
}
```

**Логи:**
```
Repository: querying database with skip = 0 take = 10
Repository: found 10 products
Repository: counting all products
Repository: total products = 50
```

#### Шаг 8: Prisma преобразует в SQL

```sql
-- Prisma преобразует findMany в SQL
SELECT * FROM products 
LIMIT 10 OFFSET 0;

-- Prisma преобразует count в SQL
SELECT COUNT(*) FROM products;
```

#### Шаг 9: PostgreSQL выполняет запросы

```
PostgreSQL получает SQL запросы
Выполняет SELECT * FROM products LIMIT 10 OFFSET 0
Возвращает 10 строк
Выполняет SELECT COUNT(*) FROM products
Возвращает 50
```

#### Шаг 10: Данные возвращаются обратно

```
PostgreSQL → Prisma → Repository → Service → Controller → Express → Клиент
```

#### Шаг 11: HTTP Ответ

```json
HTTP/1.1 200 OK
Content-Type: application/json

{
  "success": true,
  "data": {
    "data": [
      {
        "id": "prod-1",
        "name": "Product 1",
        "price": 100,
        "category": {
          "id": "cat-1",
          "name": "Category 1"
        }
      },
      // ... еще 9 товаров
    ],
    "total": 50,
    "page": 1,
    "limit": 10,
    "totalPages": 5
  }
}
```

#### Шаг 12: Клиент получает ответ

```typescript
// Фронтенд (React)
const response = await fetch('http://localhost:3000/api/products?page=1&limit=10');
const data = await response.json();

console.log(data);
// {
//   success: true,
//   data: {
//     data: [...],
//     total: 50,
//     page: 1,
//     limit: 10,
//     totalPages: 5
//   }
// }
```

---

## Пример 2: POST запрос

### 📋 Задача: Создать новый товар

**HTTP Запрос:**
```
POST /api/products
Host: localhost:3000
Content-Type: application/json

{
  "name": "New Product",
  "price": 150,
  "categoryId": "cat-1"
}
```

### 🔄 Пошаговый поток

#### Шаг 1-3: Middleware обрабатывают запрос

```typescript
// express.json() парсит тело запроса
req.body = {
  name: "New Product",
  price: 150,
  categoryId: "cat-1"
}
```

#### Шаг 4-5: Controller обрабатывает запрос

```typescript
async createProduct(req: Request, res: Response, next: NextFunction) {
  try {
    // Получение данных из тела запроса
    const product = await productService.createProduct(req.body);
    
    res.status(201).json({
      success: true,
      data: product,
      message: 'Продукт успешно создан'
    });
  } catch (error) {
    next(error);
  }
}
```

#### Шаг 6: Service валидирует и создает

```typescript
async createProduct(data: any) {
  // Валидация
  if (!data.name) {
    throw new ValidationError('Название не может быть пустым');
  }
  if (!data.price) {
    throw new ValidationError('Цена не может быть пустой');
  }
  if (data.price <= 0) {
    throw new ValidationError('Цена должна быть больше 0');
  }
  if (!data.categoryId) {
    throw new ValidationError('Категория не может быть пустой');
  }

  // Проверка существования категории
  const category = await categoryRepository.findById(data.categoryId);
  if (!category) {
    throw new NotFoundError(`Категория с ID ${data.categoryId} не найдена`);
  }

  // Создание товара
  return productRepository.create(data);
}
```

#### Шаг 7: Repository создает в БД

```typescript
async create(data: any) {
  return prisma.product.create({
    data,
    include: {
      category: true,
    },
  });
}
```

#### Шаг 8: SQL запрос

```sql
INSERT INTO products (id, name, price, categoryId, createdAt, updatedAt)
VALUES ('prod-new', 'New Product', 150, 'cat-1', NOW(), NOW());
```

#### Шаг 9: HTTP Ответ

```json
HTTP/1.1 201 Created
Content-Type: application/json

{
  "success": true,
  "data": {
    "id": "prod-new",
    "name": "New Product",
    "price": 150,
    "categoryId": "cat-1",
    "category": {
      "id": "cat-1",
      "name": "Category 1"
    },
    "createdAt": "2024-02-13T14:30:00Z",
    "updatedAt": "2024-02-13T14:30:00Z"
  },
  "message": "Продукт успешно создан"
}
```

---

## Пример 3: Обработка ошибок

### 📋 Задача: Получить товар с неверным ID

**HTTP Запрос:**
```
GET /api/products/invalid-id
Host: localhost:3000
```

### 🔄 Пошаговый поток

#### Шаг 1-5: Запрос доходит до Controller

```typescript
async getProductById(req: Request, res: Response, next: NextFunction) {
  try {
    const { id } = req.params;  // id = "invalid-id"
    const product = await productService.getProductById(id);
    
    res.json({
      success: true,
      data: product
    });
  } catch (error) {
    next(error);  // Передаем ошибку в error handler
  }
}
```

#### Шаг 6: Service валидирует и вызывает репозиторий

```typescript
async getProductById(id: string) {
  if (!id) {
    throw new ValidationError('ID не может быть пустым');
  }

  const product = await productRepository.findById(id);
  
  if (!product) {
    throw new NotFoundError(`Продукт с ID ${id} не найден`);
  }

  return product;
}
```

**Выбрасывается ошибка:**
```
NotFoundError: Продукт с ID invalid-id не найден
statusCode: 404
```

#### Шаг 7: Controller ловит ошибку

```typescript
catch (error) {
  next(error);  // Передаем ошибку в error handler middleware
}
```

#### Шаг 8: Error Handler обрабатывает ошибку

```typescript
export const errorHandler = (
  err: Error | AppError,
  req: Request,
  res: Response,
  next: NextFunction
) => {
  logger.error('Error occurred:', err);

  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      success: false,
      error: err.message,
    });
  }

  res.status(500).json({
    success: false,
    error: 'Внутренняя ошибка сервера',
  });
};
```

#### Шаг 9: HTTP Ответ

```json
HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "success": false,
  "error": "Продукт с ID invalid-id не найден"
}
```

#### Шаг 10: Клиент получает ошибку

```typescript
// Фронтенд (React)
const response = await fetch('http://localhost:3000/api/products/invalid-id');
const data = await response.json();

if (!response.ok) {
  console.error('Error:', data.error);
  // Error: Продукт с ID invalid-id не найден
}
```

---

## Диаграммы потока

### Полный поток GET запроса

```
┌─────────────────────────────────────────────────────────────┐
│ 1. КЛИЕНТ отправляет запрос                                 │
│    GET /api/products?page=1&limit=10                        │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 2. EXPRESS получает запрос                                  │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 3. MIDDLEWARE обрабатывают                                  │
│    - express.json()                                         │
│    - corsMiddleware                                         │
│    - loggingMiddleware                                      │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 4. ROUTER находит маршрут                                   │
│    GET /api/products → productController.getAllProducts     │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 5. CONTROLLER обрабатывает                                  │
│    - Получает параметры: page=1, limit=10                  │
│    - Вызывает productService.getAllProducts(1, 10)          │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 6. SERVICE выполняет бизнес-логику                          │
│    - Валидирует параметры                                   │
│    - Вычисляет skip = 0                                     │
│    - Вызывает productRepository.findAll(0, 10)              │
│    - Вызывает productRepository.count()                     │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 7. REPOSITORY работает с БД                                 │
│    - prisma.product.findMany({ skip: 0, take: 10 })        │
│    - prisma.product.count()                                 │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 8. PRISMA преобразует в SQL                                 │
│    SELECT * FROM products LIMIT 10 OFFSET 0;               │
│    SELECT COUNT(*) FROM products;                          │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 9. POSTGRESQL выполняет запросы                             │
│    - Возвращает 10 товаров                                  │
│    - Возвращает total = 50                                  │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 10. REPOSITORY возвращает результат                         │
│     [products[], total]                                     │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 11. SERVICE трансформирует данные                           │
│     { data, total, page, limit, totalPages }                │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 12. CONTROLLER формирует JSON ответ                         │
│     { success: true, data: result }                         │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 13. EXPRESS отправляет ответ                                │
│     HTTP 200 OK                                             │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ 14. КЛИЕНТ получает ответ                                   │
│     { success: true, data: { ... } }                        │
└─────────────────────────────────────────────────────────────┘
```

### Поток обработки ошибок

```
Ошибка возникает в Service
    ↓
throw new NotFoundError(...)
    ↓
Controller ловит в try-catch
    ↓
next(error)
    ↓
Error Handler Middleware
    ↓
err instanceof AppError?
    ├─ YES → res.status(err.statusCode).json(...)
    └─ NO → res.status(500).json(...)
    ↓
HTTP Ответ с ошибкой
    ↓
Клиент получает ошибку
```

### Взаимодействие компонентов

```
┌──────────────┐
│   КЛИЕНТ     │
│   (React)    │
└──────┬───────┘
       │ HTTP запрос
       ▼
┌──────────────────────────────────────┐
│         EXPRESS.JS СЕРВЕР            │
│  ┌────────────────────────────────┐  │
│  │ MIDDLEWARE                     │  │
│  │ - CORS, JSON, Логирование      │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ ROUTES                         │  │
│  │ - Маршруты                     │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ CONTROLLERS                    │  │
│  │ - Обработка HTTP               │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ SERVICES                       │  │
│  │ - Бизнес-логика                │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ REPOSITORIES                   │  │
│  │ - Работа с БД                  │  │
│  └────────────────────────────────┘  │
└──────┬───────────────────────────────┘
       │ SQL запросы
       ▼
┌──────────────────────────────────────┐
│      PRISMA ORM                      │
│  - Преобразование в SQL              │
└──────┬───────────────────────────────┘
       │ SQL запросы
       ▼
┌──────────────────────────────────────┐
│      POSTGRESQL                      │
│  - Таблицы, индексы, связи           │
└──────────────────────────────────────┘
```

---

## Ключевые моменты

✅ Данные движутся вниз через слои (запрос)
✅ Данные движутся вверх через слои (ответ)
✅ Каждый слой добавляет свою функциональность
✅ Ошибки обрабатываются на каждом уровне
✅ Middleware выполняются в порядке добавления
✅ Error handler должен быть последним

---

## Следующие шаги

1. Прочитайте **PRACTICAL_EXAMPLES.md** - практикуйтесь с примерами
2. Прочитайте **FRONTEND_BACKEND_INTEGRATION.md** - свяжите фронтенд и бэкенд
