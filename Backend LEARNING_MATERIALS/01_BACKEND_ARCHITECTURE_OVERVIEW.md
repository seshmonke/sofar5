# 🏗️ Обзор архитектуры бэкенда

## Содержание
1. [Общая архитектура](#общая-архитектура)
2. [Слои приложения](#слои-приложения)
3. [Поток запроса](#поток-запроса)
4. [Компоненты системы](#компоненты-системы)
5. [Диаграммы](#диаграммы)

---

## Общая архитектура

Ваше приложение построено на **слоистой архитектуре (Layered Architecture)**, которая разделяет ответственность между разными компонентами. Это делает код:
- **Мо��ульным** - каждый слой отвечает за одно
- **Тестируемым** - легко писать тесты для каждого слоя
- **Масштабируемым** - легко добавлять новые функции
- **Поддерживаемым** - легко найти и исправить ошибки

---

## Слои приложения

### 📊 Архитектурная пирамида

```
┌─────────────────────────────────────┐
│      HTTP Запросы от клиента        │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  ROUTES СЛОЙ (Маршруты)             │
│  - Определение endpoints            │
│  - Swagger документация              │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  MIDDLEWARE СЛОЙ                    │
│  - CORS, JSON парсинг               │
│  - Логирование, обработка ошибок    │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  CONTROLLERS СЛОЙ                   │
│  - Обработка HTTP запросов          │
│  - Валидация параметров             │
│  - Формирование ответов             │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  SERVICES СЛОЙ                      │
│  - Бизнес-логика                    │
│  - Валидация данных                 │
│  - Обработка ошибок                 │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  REPOSITORIES СЛОЙ                  │
│  - Работа с базой данных            │
│  - CRUD операции                    │
│  - Prisma ORM                       │
└──────────────┬───────────────���──────┘
               │
┌──────────────▼──────────────────────┐
│  DATABASE СЛОЙ                      │
│  - PostgreSQL                       │
│  - Таблицы, индексы, связи          │
└─────────────────────────────────────┘
```

### 1️⃣ ROUTES слой (Маршруты)

**Файл:** `src/routes/products.ts`

**Ответственность:**
- Определение HTTP endpoints (GET, POST, PUT, DELETE)
- Связывание URL с обработчиками
- Swagger документация

**Пример:**
```typescript
router.get('/', (req, res, next) => productController.getAllProducts(req, res, next));
```

**Что происходит:**
- Когда клиент отправляет `GET /api/products`, этот маршрут перенаправляет запрос в контроллер

---

### 2️⃣ MIDDLEWARE слой

**Файлы:** `src/middleware/cors.ts`, `src/middleware/errorHandler.ts`

**Ответственность:**
- Обработка CORS (кроссдоменные запросы)
- Парсинг JSON
- Логирование запросов
- Обработка ошибок

**Пример:**
```typescript
app.use(express.json()); // Парсит JSON из тела запроса
app.use(corsMiddleware); // Разрешает запросы с фронтенда
app.use(errorHandler);   // Ловит все ошибки
```

**Что происходит:**
- Middleware выполняются в порядке, в котором они добавлены
- Каждый middleware может обработать запрос или передать его дальше

---

### 3️⃣ CONTROLLERS слой (Контроллеры)

**Файл:** `src/controllers/productController.ts`

**Ответственность:**
- Получение параметров из запроса
- Вызов сервиса для обработки
- Формирование HTTP ответа
- Обработка ошибок

**Пример:**
```typescript
async getAllProducts(req: Request, res: Response, next: NextFunction) {
  try {
    const page = parseInt(req.query.page as string) || 1;
    const limit = parseInt(req.query.limit as string) || 10;
    
    const result = await productService.getAllProducts(page, limit);
    
    res.status(HTTP_STATUS.OK).json({
      success: true,
      data: result,
    });
  } catch (error) {
    next(error); // Передаёт ошибку в error handler
  }
}
```

**Что происходит:**
1. Контроллер получает параметры из запроса
2. Вызывает сервис для обработки
3. Возвращает результат клиенту в формате JSON

---

### 4️⃣ SERVICES слой (Сервисы)

**Файл:** `src/services/productService.ts`

**Ответственность:**
- Бизнес-логика приложения
- Валидация данных
- Координация работы репозиториев
- Обработка ошибок

**Пример:**
```typescript
async getAllProducts(page: number = 1, limit: number = LIMITS.DEFAULT_PAGE_SIZE) {
  // Валидация
  if (page < 1 || limit < 1) {
    throw new ValidationError('Page и limit должны быть больше 0');
  }

  // Бизнес-логика
  const skip = (page - 1) * limit;
  const [products, total] = await Promise.all([
    productRepository.findAll(skip, limit),
    productRepository.count(),
  ]);

  // Формирование результата
  return {
    data: products,
    total,
    page,
    limit,
    totalPages: Math.ceil(total / limit),
  };
}
```

**Что происходит:**
1. Сервис валидирует входные данные
2. Выполняет бизнес-логику
3. Вызывает репозиторий для работы с БД
4. Возвращает результат контроллеру

---

### 5️⃣ REPOSITORIES слой (Репозитории)

**Файл:** `src/repositories/productRepository.ts`

**Ответственность:**
- Работа с базой данных
- CRUD операции (Create, Read, Update, Delete)
- Использование Prisma ORM

**Пример:**
```typescript
async findAll(skip: number = 0, take: number = 10) {
  return prisma.product.findMany({
    skip,
    take,
    include: {
      category: true,
    },
  });
}
```

**Что происходит:**
1. Репозиторий использует Prisma для работы с БД
2. Prisma преобразует это в SQL запрос
3. PostgreSQL выполняет запрос
4. Результат возвращается репозиторию

---

### 6️⃣ DATABASE слой (База данных)

**Файл:** `prisma/schema.prisma`

**Ответственность:**
- Хранение данных
- Определение структуры таблиц
- Связи между таблицами

**Пример:**
```prisma
model Product {
  id          String   @id @default(cuid())
  name        String
  price       Float
  category    Category @relation(fields: [categoryId], references: [id])
  categoryId  String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("products")
}
```

---

## Поток запроса

### Пример: Получение всех товаров

```
1. КЛИЕНТ отправляет запрос
   GET /api/products?page=1&limit=10

2. EXPRESS получает запрос
   ↓

3. MIDDLEWARE обрабатывают запрос
   - CORS middleware проверяет источник
   - JSON middleware парсит тело (если есть)
   - Логирование middleware логирует запрос
   ↓

4. ROUTER находит подходящий маршрут
   GET /api/products → productController.getAllProducts
   ↓

5. CONTROLLER обрабатывает запрос
   - Извлекает параметры: page=1, limit=10
   - Вызывает productService.getAllProducts(1, 10)
   ↓

6. SERVICE выполняет бизнес-логику
   - Валидирует параметры
   - Вычисляет skip = (1-1)*10 = 0
   - Вызывает productRepository.findAll(0, 10)
   - Вызывает productRepository.count()
   ↓

7. REPOSITORY работает с БД
   - Prisma преобразует в SQL:
     SELECT * FROM products LIMIT 10 OFFSET 0;
     SELECT COUNT(*) FROM products;
   - PostgreSQL выполняет запросы
   - Результаты возвращаются
   ↓

8. SERVICE формирует ответ
   {
     data: [...],
     total: 50,
     page: 1,
     limit: 10,
     totalPages: 5
   }
   ↓

9. CONTROLLER отправляет HTTP ответ
   HTTP 200 OK
   {
     "success": true,
     "data": {
       "data": [...],
       "total": 50,
       "page": 1,
       "limit": 10,
       "totalPages": 5
     }
   }
   ↓

10. КЛИЕНТ получает ответ
    Фронтенд обрабатывает данные и отображает товары
```

---

## Компоненты системы

### 🔧 Express.js
**Что это:** Веб-фреймворк для Node.js

**Зачем нужен:**
- Обработка HTTP запросов
- Маршрутизация
- Middleware система

**В вашем проекте:**
```typescript
const app = express();
app.use(express.json());
app.use('/api', apiRoutes);
```

---

### 🗄️ Prisma ORM
**Что это:** Object-Relational Mapping - абстракция над БД

**Зачем нужна:**
- Не писать SQL вручную
- Типизация (TypeScript)
- Миграции БД
- Валидация данных

**В вашем проекте:**
```typescript
const products = await prisma.product.findMany({
  skip: 0,
  take: 10,
  include: { category: true }
});
```

---

### 🐘 PostgreSQL
**Что это:** Реляционная база данных

**Зачем нужна:**
- Хранение данных
- Сложные запросы
- Транзакции
- Индексы для производительности

**В вашем проекте:**
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/assortiShop"
```

---

### 🐳 Docker
**Что это:** Контейнеризация приложения

**Зачем нужен:**
- Одинаковое окружение везде
- Легко запустить на любой машине
- Изоляция сервисов

**В вашем проекте:**
```yaml
services:
  postgres:
    image: postgres:16-alpine
  backend:
    build: .
```

---

## Диаграммы

### Взаимодействие компонентов

```
┌─────────────────────────────────────────────────────────────┐
│                    ФРОНТЕНД (React)                         │
│  - Отправляет HTTP запросы                                  │
│  - Получает JSON ответы                                     │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP запросы
                     │ (GET, POST, PUT, DELETE)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  EXPRESS.JS СЕРВЕР                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ MIDDLEWARE (CORS, JSON, Логирование, Ошибки)        │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ROUTES (Маршруты)                                    │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ CONTROLLERS (Обработка запросов)                     │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ SERVICES (Бизнес-логика)                            │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ REPOSITORIES (Работа с БД)                          │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────┬────────────────────────────────────────┘
                     │ SQL запросы
                     │ (SELECT, INSERT, UPDATE, DELETE)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  PRISMA ORM                                 │
│  - Преобразует JavaScript в SQL                            │
│  - Типизация                                               │
└────────────────────┬────────────────────────────────────────┘
                     │ SQL запросы
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  POSTGRESQL                                 │
│  - Таблицы: products, categories, users, carts             │
│  - Индексы, связи, триггеры                                │
└─────────────────────────────────────────────────────────────┘
```

### Жизненный цикл запроса

```
Клиент                Express              Service           Repository        БД
  │                     │                    │                  │               │
  │─ GET /api/products ─>                    │                  │               │
  │                     │                    │                  │               │
  │                     │─ Middleware ──>    │                  │               │
  │                     │                    │                  │               │
  │                     │─ Controller ──>    │                  │               │
  │                     │                    │                  │               │
  │                     │                    │─ Валидация ──>   │               │
  │                     │                    │                  │               │
  │                     │                    │─ findAll() ──>   │               │
  │                     │                    │                  │               │
  │                     │                    │                  │─ Prisma ──>   │
  │                     │                    │                  │               │
  │                     │                    │                  │               │─ SQL ──>
  │                     │                    │                  │               │
  │                     │                    │                  │<─ Результат ─│
  │                     │                    │                  │               │
  │                     │                    │<─ Данные ────────│               │
  │                     │                    │                  │               │
  │                     │<─ JSON ────────────│                  │               │
  │                     │                    │                  │               │
  │<─ HTTP 200 ────────│                    │                  │               │
  │                     │                    │                  │               │
```

---

## Ключевые концепции

### 🎯 Разделение ответственности
Каждый слой отвечает за одно:
- **Routes** - определение endpoints
- **Controllers** - обработка HTTP
- **Services** - бизнес-логика
- **Repositories** - работа с БД

### 🔄 Поток данных
Данные движутся в одном направлении:
- Запрос идёт вниз (от Routes к БД)
- Ответ идёт вверх (от БД к Routes)

### ⚠️ Обработка ошибок
Ошибки обрабатываются на каждом уровне:
- **Services** - валидирует данные
- **Controllers** - ловит ошибки
- **Middleware** - глобальная обработка ошибок

### 📦 Модульность
Каждый компонент независим:
- Легко тестировать
- Легко переиспользовать
- Легко заменить

---

## Следующие шаги

1. Прочитайте **EXPRESS_DEEP_DIVE.md** - поймите как работает Express
2. Прочитайте **LAYERED_ARCHITECTURE_EXPLAINED.md** - разберитесь с каждым слоем
3. Прочитайте **PRISMA_POSTGRESQL_GUIDE.md** - поймите работу с БД
4. Прочитайте **DATA_FLOW_BACKEND.md** - проследите полный путь запроса

---

## Резюме

✅ Ваше приложение использует **слоистую архитектуру**
✅ Каждый слой отвечает за одно
✅ Данные движутся от Routes к БД и обратно
✅ Ошибки обрабатываются на каждом уровне
✅ Это делает код модульным, тестируемым и масштабируемым
